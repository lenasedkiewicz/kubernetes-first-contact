spring.application.name=taskboard-api

# Kafka - use embedded
spring.kafka.bootstrap-servers=${spring.embedded.kafka.brokers}

# Tasks Service - mock URL for tests
tasks.service.base-url=http://localhost:8082
